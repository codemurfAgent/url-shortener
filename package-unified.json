{
  "name": "url-shortener-unified",
  "version": "2.1.0",
  "description": "Production URL shortener with unified PostgreSQL database - solves data isolation and redirect issues",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for serverless functions'",
    "start": "echo 'Use Vercel to deploy serverless functions'",
    "dev": "echo 'Use Vercel dev for local development'",
    "deploy": "vercel --prod",
    "logs": "vercel logs",
    "test-local": "node test-unified-api.js"
  },
  "keywords": ["url-shortener", "vercel", "serverless", "postgresql", "analytics"],
  "author": "Dev Team",
  "license": "MIT",
  "dependencies": {
    "@vercel/postgres": "^0.5.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/url-shortener-unified"
  },
  "bugs": {
    "url": "https://github.com/your-username/url-shortener-unified/issues"
  },
  "homepage": "https://github.com/your-username/url-shortener-unified#readme"
}